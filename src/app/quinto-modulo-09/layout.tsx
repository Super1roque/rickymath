import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 9 — Media y estadística',
  description: 'Sumá, repartí en partes iguales y encontrá el promedio, temática Minecraft.',
}

export default function QuintoModulo09Layout({ children }: { children: React.ReactNode }) {
  return children
}
