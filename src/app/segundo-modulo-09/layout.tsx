import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 9 — El reloj y la hora',
  description: 'Leé la hora en relojes analógicos, temática Minecraft.',
}

export default function SegundoModulo09Layout({ children }: { children: React.ReactNode }) {
  return children
}
