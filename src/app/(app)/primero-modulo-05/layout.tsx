import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 5 — Restar con bloques',
  description: 'Tachá los bloques que se van y escribí cuántos quedan, temática Minecraft.',
}

export default function PrimeroModulo05Layout({ children }: { children: React.ReactNode }) {
  return children
}
