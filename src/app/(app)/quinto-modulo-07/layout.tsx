import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 7 — Volumen',
  description: 'Contá los bloques de cada construcción y calculá el volumen, temática Minecraft.',
}

export default function QuintoModulo07Layout({ children }: { children: React.ReactNode }) {
  return children
}
