import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 6 — Decimales',
  description: 'Leé y comparó números decimales con el delfín que mide la profundidad, temática Minecraft.',
}

export default function CuartoModulo06Layout({ children }: { children: React.ReactNode }) {
  return children
}
