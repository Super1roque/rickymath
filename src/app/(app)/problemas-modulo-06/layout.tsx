import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 06 — La gran construcción',
  description: 'Ayudá al gólem de hierro a medir todo antes de construir — temática Minecraft.',
}

export default function ProblemasModulo06Layout({ children }: { children: React.ReactNode }) {
  return children
}
