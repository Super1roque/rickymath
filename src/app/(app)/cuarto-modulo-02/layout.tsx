import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 2 — Operaciones combinadas',
  description: 'Resolvé operaciones combinadas respetando el orden: paréntesis, multiplicación/división, suma/resta.',
}

export default function CuartoModulo02Layout({ children }: { children: React.ReactNode }) {
  return children
}
