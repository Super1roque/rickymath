import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Tabla del 4',
  description: 'Aprendé la tabla de multiplicar del 4 y practicá, temática Minecraft.',
}

export default function TablasModulo04Layout({ children }: { children: React.ReactNode }) {
  return children
}
