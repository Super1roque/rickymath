import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Tabla del 7',
  description: 'Aprendé la tabla de multiplicar del 7 y practicá, temática Minecraft.',
}

export default function TablasModulo07Layout({ children }: { children: React.ReactNode }) {
  return children
}
