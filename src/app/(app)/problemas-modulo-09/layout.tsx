import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 09 — La torre del Fin',
  description: 'Ayudá al enderman a calcular el volumen de sus construcciones en el vacío — temática Minecraft.',
}

export default function ProblemasModulo09Layout({ children }: { children: React.ReactNode }) {
  return children
}
