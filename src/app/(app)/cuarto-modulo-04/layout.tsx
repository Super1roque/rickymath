import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 4 — Divisores',
  description: 'Marcá todos los divisores de cada número, temática Minecraft.',
}

export default function CuartoModulo04Layout({ children }: { children: React.ReactNode }) {
  return children
}
