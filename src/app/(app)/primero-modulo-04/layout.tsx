import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 4 — Sumar con bloques',
  description: 'Sumá dos grupos de bloques y escribí el total, temática Minecraft.',
}

export default function PrimeroModulo04Layout({ children }: { children: React.ReactNode }) {
  return children
}
