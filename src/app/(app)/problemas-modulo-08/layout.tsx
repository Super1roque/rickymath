import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 08 — La tienda del Nether',
  description: 'Ayudá a calcular descuentos y recargos en la tienda del piglin — porcentajes, temática Minecraft.',
}

export default function ProblemasModulo08Layout({ children }: { children: React.ReactNode }) {
  return children
}
