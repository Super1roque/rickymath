import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 05 — El mercado de esmeraldas',
  description: 'Comprá, vendé y no te dejes engañar en el mercado del vendedor ambulante — temática Minecraft.',
}

export default function ProblemasModulo05Layout({ children }: { children: React.ReactNode }) {
  return children
}
