import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 6 — Secuencias numéricas',
  description: 'Descubrí el patrón y completá la secuencia, temática Minecraft.',
}

export default function SegundoModulo06Layout({ children }: { children: React.ReactNode }) {
  return children
}
