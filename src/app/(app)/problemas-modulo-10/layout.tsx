import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 10 — La misión final',
  description: 'El desafío final que junta todo lo aprendido en la serie Problemas — temática Minecraft.',
}

export default function ProblemasModulo10Layout({ children }: { children: React.ReactNode }) {
  return children
}
