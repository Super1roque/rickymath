import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 4 — Fracciones simples',
  description: 'Observá la parte coloreada y escribí la fracción, temática Minecraft.',
}

export default function TerceroModulo04Layout({ children }: { children: React.ReactNode }) {
  return children
}
