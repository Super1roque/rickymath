import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 10 — Misión final',
  description: 'Resolvé problemas escribiendo la ecuación completa, temática Minecraft.',
}

export default function SegundoModulo10Layout({ children }: { children: React.ReactNode }) {
  return children
}
