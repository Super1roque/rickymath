import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 9 — Gráficos de barras',
  description: 'Leé cada gráfico y respondé las preguntas, temática Minecraft.',
}

export default function TerceroModulo09Layout({ children }: { children: React.ReactNode }) {
  return children
}
