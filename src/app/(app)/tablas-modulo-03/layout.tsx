import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Tabla del 3',
  description: 'Aprendé la tabla de multiplicar del 3 y practicá, temática Minecraft.',
}

export default function TablasModulo03Layout({ children }: { children: React.ReactNode }) {
  return children
}
