import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 3 — Tablas de multiplicar',
  description: 'Practicá las tablas de multiplicar contra el reloj, temática Minecraft.',
}

export default function TerceroModulo03Layout({ children }: { children: React.ReactNode }) {
  return children
}
