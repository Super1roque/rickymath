import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 10 — Misión final',
  description: 'Problemas de suma y resta para cerrar el Grado Primero, temática Minecraft.',
}

export default function PrimeroModulo10Layout({ children }: { children: React.ReactNode }) {
  return children
}
