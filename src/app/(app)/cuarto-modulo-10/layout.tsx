import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 10 — Misión final',
  description: 'Tres misiones finales de resolución de problemas, temática Minecraft.',
}

export default function CuartoModulo10Layout({ children }: { children: React.ReactNode }) {
  return children
}
