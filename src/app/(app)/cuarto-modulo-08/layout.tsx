import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 8 — Perímetro y área',
  description: 'Calculá el perímetro y el área de cada terreno cercado, temática Minecraft.',
}

export default function CuartoModulo08Layout({ children }: { children: React.ReactNode }) {
  return children
}
