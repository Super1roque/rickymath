import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 7 — Ángulos y figuras',
  description: 'Observá cada ángulo y marcá si es recto, agudo u obtuso, temática Minecraft.',
}

export default function TerceroModulo07Layout({ children }: { children: React.ReactNode }) {
  return children
}
