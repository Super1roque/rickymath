import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 5 — Decimales × y ÷ por 10, 100 y 1000',
  description: 'Observá hacia dónde salta la coma y escribí el resultado, temática Minecraft.',
}

export default function QuintoModulo05Layout({ children }: { children: React.ReactNode }) {
  return children
}
