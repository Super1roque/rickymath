import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 07 — El pastel de la abeja',
  description: 'Ayudá a la abeja a repartir sus pasteles de miel en partes justas — fracciones, temática Minecraft.',
}

export default function ProblemasModulo07Layout({ children }: { children: React.ReactNode }) {
  return children
}
