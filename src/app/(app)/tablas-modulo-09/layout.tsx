import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Tabla del 9',
  description: 'Aprendé la tabla de multiplicar del 9 y practicá, temática Minecraft.',
}

export default function TablasModulo09Layout({ children }: { children: React.ReactNode }) {
  return children
}
