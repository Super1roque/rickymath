import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 3 — Resta prestando',
  description: 'Resolvé restas de dos cifras pidiendo prestado a las decenas, temática Minecraft.',
}

export default function SegundoModulo03Layout({ children }: { children: React.ReactNode }) {
  return children
}
