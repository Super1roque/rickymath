import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 10 — Misión final',
  description: 'Resolución de problemas de varios pasos, temática Minecraft.',
}

export default function TerceroModulo10Layout({ children }: { children: React.ReactNode }) {
  return children
}
