import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 1 — Valor posicional',
  description: 'Contá decenas y unidades y escribí el número, temática Minecraft.',
}

export default function SegundoModulo01Layout({ children }: { children: React.ReactNode }) {
  return children
}
