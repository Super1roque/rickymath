import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 1 — Números naturales grandes',
  description: 'Separá números grandes en órdenes y escribilos con letras, temática Minecraft.',
}

export default function CuartoModulo01Layout({ children }: { children: React.ReactNode }) {
  return children
}
