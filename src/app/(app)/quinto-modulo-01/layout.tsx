import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 1 — Números hasta el millón',
  description: 'Separá números hasta el millón en órdenes y escribilos con letras, temática Minecraft.',
}

export default function QuintoModulo01Layout({ children }: { children: React.ReactNode }) {
  return children
}
