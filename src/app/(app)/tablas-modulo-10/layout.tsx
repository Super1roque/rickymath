import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Tabla del 10',
  description: 'Aprendé la tabla de multiplicar del 10 y practicá — ¡dominaste las tablas!, temática Minecraft.',
}

export default function TablasModulo10Layout({ children }: { children: React.ReactNode }) {
  return children
}
