import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 5 — División básica',
  description: 'Repartí en partes iguales y escribí cuántos toca a cada uno, temática Minecraft.',
}

export default function SegundoModulo05Layout({ children }: { children: React.ReactNode }) {
  return children
}
