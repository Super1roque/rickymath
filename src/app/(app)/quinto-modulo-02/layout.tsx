import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 2 — Potencias y raíz cuadrada',
  description: 'Construí cuadrados y cubos de bloques y calculá potencias y raíces cuadradas, temática Minecraft.',
}

export default function QuintoModulo02Layout({ children }: { children: React.ReactNode }) {
  return children
}
