import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 5 — El dinero',
  description: 'Calculá precios y vueltos en esmeraldas en el mercado del Piglin, temática Minecraft.',
}

export default function TerceroModulo05Layout({ children }: { children: React.ReactNode }) {
  return children
}
