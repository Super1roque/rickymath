import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 3 — Operaciones combinadas con potencias',
  description: 'Resolvé operaciones combinadas respetando el orden: paréntesis, potencias, × y ÷, + y −, temática Minecraft.',
}

export default function QuintoModulo03Layout({ children }: { children: React.ReactNode }) {
  return children
}
