import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 10 — Misión final y diploma',
  description: 'Tres misiones finales: leé, planificá, resolvé y revisá — y ganá tu diploma, temática Minecraft.',
}

export default function QuintoModulo10Layout({ children }: { children: React.ReactNode }) {
  return children
}
