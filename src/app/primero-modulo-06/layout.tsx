import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 6 — Figuras geométricas',
  description: 'Reconocé cuadrado, triángulo, rectángulo, círculo, rombo y estrella, temática Minecraft.',
}

export default function PrimeroModulo06Layout({ children }: { children: React.ReactNode }) {
  return children
}
