import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 7 — Área',
  description: 'Contá los bloques de cada terreno y calculá su área, temática Minecraft.',
}

export default function CuartoModulo07Layout({ children }: { children: React.ReactNode }) {
  return children
}
