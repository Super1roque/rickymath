import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 3 — Mayor, menor o igual',
  description: 'Comparás cantidades con >, < e =, temática Minecraft.',
}

export default function PrimeroModulo03Layout({ children }: { children: React.ReactNode }) {
  return children
}
