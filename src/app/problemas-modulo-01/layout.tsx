import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 01 — La primera cosecha',
  description: 'Ayudá a Steve a contar todo lo que recoge de su huerto, temática Minecraft.',
}

export default function ProblemasModulo01Layout({ children }: { children: React.ReactNode }) {
  return children
}
