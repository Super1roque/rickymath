import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 2 — Suma llevando',
  description: 'Resolvé sumas de dos cifras llevando a las decenas, temática Minecraft.',
}

export default function SegundoModulo02Layout({ children }: { children: React.ReactNode }) {
  return children
}
