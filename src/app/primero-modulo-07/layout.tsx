import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 7 — Largo o corto',
  description: 'Comparás objetos y marcás cuál es más largo, temática Minecraft.',
}

export default function PrimeroModulo07Layout({ children }: { children: React.ReactNode }) {
  return children
}
