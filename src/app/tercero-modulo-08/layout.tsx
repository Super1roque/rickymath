import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 8 — Medidas',
  description: 'Elegí la unidad correcta y completá las equivalencias, temática Minecraft.',
}

export default function TerceroModulo08Layout({ children }: { children: React.ReactNode }) {
  return children
}
