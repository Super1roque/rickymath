import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 03 — La granja de pollos',
  description: 'Ayudá a la gallina a contar todo lo que hay en su granja — multiplicá!, temática Minecraft.',
}

export default function ProblemasModulo03Layout({ children }: { children: React.ReactNode }) {
  return children
}
