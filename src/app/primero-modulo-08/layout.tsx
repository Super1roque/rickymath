import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 8 — Las monedas',
  description: 'Contá las esmeraldas de cada cofre y escribí cuántas hay, temática Minecraft.',
}

export default function PrimeroModulo08Layout({ children }: { children: React.ReactNode }) {
  return children
}
