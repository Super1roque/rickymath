import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 2 — División',
  description: 'Practicá divisiones con cociente y resto, temática Minecraft.',
}

export default function TerceroModulo02Layout({ children }: { children: React.ReactNode }) {
  return children
}
