import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 7 — Lados y vértices',
  description: 'Contá los lados y los vértices de cada figura, temática Minecraft.',
}

export default function SegundoModulo07Layout({ children }: { children: React.ReactNode }) {
  return children
}
