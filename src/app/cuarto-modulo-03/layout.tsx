import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 3 — Múltiplos',
  description: 'Marcá los múltiplos en el camino de la rana, temática Minecraft.',
}

export default function CuartoModulo03Layout({ children }: { children: React.ReactNode }) {
  return children
}
