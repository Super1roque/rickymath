import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 6 — Porcentajes',
  description: 'Calculá porcentajes y descuentos en las ofertas del vendedor ambulante, temática Minecraft.',
}

export default function QuintoModulo06Layout({ children }: { children: React.ReactNode }) {
  return children
}
