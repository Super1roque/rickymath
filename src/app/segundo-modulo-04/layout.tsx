import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 4 — Multiplicación inicial',
  description: 'Contá grupos iguales y escribí la multiplicación, temática Minecraft.',
}

export default function SegundoModulo04Layout({ children }: { children: React.ReactNode }) {
  return children
}
