import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 9 — Gráficos',
  description: 'Leé los gráficos del arrecife y respondé, temática Minecraft.',
}

export default function CuartoModulo09Layout({ children }: { children: React.ReactNode }) {
  return children
}
