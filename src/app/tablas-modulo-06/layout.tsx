import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Tabla del 6',
  description: 'Aprendé la tabla de multiplicar del 6 y practicá, temática Minecraft.',
}

export default function TablasModulo06Layout({ children }: { children: React.ReactNode }) {
  return children
}
