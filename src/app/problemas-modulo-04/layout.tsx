import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Misión 04 — Repartir el botín',
  description: 'Ayudá al lobo a repartir el tesoro en partes iguales, sin trampas — temática Minecraft.',
}

export default function ProblemasModulo04Layout({ children }: { children: React.ReactNode }) {
  return children
}
