import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 1 — Multiplicación',
  description: 'Practicá multiplicaciones de dos cifras por una, temática Minecraft.',
}

export default function TerceroModulo01Layout({ children }: { children: React.ReactNode }) {
  return children
}
