import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Tabla del 8',
  description: 'Aprendé la tabla de multiplicar del 8 y practicá, temática Minecraft.',
}

export default function TablasModulo08Layout({ children }: { children: React.ReactNode }) {
  return children
}
