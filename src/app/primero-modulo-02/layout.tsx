import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 2 — Antes, ahora y después',
  description: 'Ordená las escenas en el orden correcto, temática Minecraft.',
}

export default function PrimeroModulo02Layout({ children }: { children: React.ReactNode }) {
  return children
}
