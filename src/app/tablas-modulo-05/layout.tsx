import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Tabla del 5',
  description: 'Aprendé la tabla de multiplicar del 5 y practicá, temática Minecraft.',
}

export default function TablasModulo05Layout({ children }: { children: React.ReactNode }) {
  return children
}
