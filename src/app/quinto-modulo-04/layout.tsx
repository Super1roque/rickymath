import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 4 — Fracciones con distinto denominador',
  description: 'Buscá el denominador común y sumá o restá fracciones, temática Minecraft.',
}

export default function QuintoModulo04Layout({ children }: { children: React.ReactNode }) {
  return children
}
