import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Actividad 8 — Ángulos y circunferencia',
  description: 'Clasificá los ángulos del monumento y medí sus círculos, temática Minecraft.',
}

export default function QuintoModulo08Layout({ children }: { children: React.ReactNode }) {
  return children
}
